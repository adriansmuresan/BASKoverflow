<h1><%= @question.title %></h1>
<p><%= @question.body %></p>
<span> @question.vote.values.sum (fix this later) </span>
<span>< @question.author.username (fix)</span>
<span><%= @question.created_at %></span>
<ul>
  <%= @question.answers.each do |answer| %>
  <li>
    <%= answer.description %>
    <span>answer.votes.value.sum(fix)</span>
    <span>answer.answerer_id (fix)</span>
    <span> answer.created_at (fix)</span>
  </li>
</ul>
