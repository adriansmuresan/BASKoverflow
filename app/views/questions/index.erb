<h1> WELCOME TO GAMEOVERFLOW !!! </h1>
<% @questions.each do |question| %>
<ul>
  <li>
    <a href="/questions/<%= question.id %>"><%= question.title %></a><br>
    <form></form>
    <span>question.votes.value.sum (fix this later)</span>
    <span> question.author.username (fix this)</span>
  </li>
</ul>
<% end %>
